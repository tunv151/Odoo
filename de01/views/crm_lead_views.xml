<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_crm_lead_form_inherit" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Nhu cầu của KH" name="requests">
                    <field name="request_ids">
                        <tree editable="bottom">
                            <field name="product_id" string="Sản phẩm" options="{'no_create': True}"/>
                            <field name="date" string="Ngày"/>
                            <field name="qty" string="Số lượng"/>
                            <field name="description" string="Mô tả"/>
                        </tree>
                    </field>
                    <group>
                        <field name="total_qty" string="Tổng số lượng"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>